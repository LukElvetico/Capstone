// Placeholder per creare un post (richiede la verifica hasPurchased)
export const createPost = (req, res) => {
    res.status(501).json({ message: "wIP" });
};

// Placeholder per ottenere tutti i post
export const getPosts = (req, res) => {
    res.status(501).json({ message: "WIP" });
};

// Placeholder per ottenere un singolo post per ID
export const getPostById = (req, res) => {
    res.status(501).json({ message: "WIP" });
};

// bONUS Placeholder per i commenti sui post
/*
export const createComment = (req, res) => {
    res.status(501).json({ message: "La funzionalità createComment per i post è in fase di implementazione." });
};

export const getCommentsByPost = (req, res) => {
    res.status(501).json({ message: "La funzionalità getCommentsByPost è in fase di implementazione." });
};
*/