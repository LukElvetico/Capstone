@base_url = http://localhost:4000
@auth_token = {{auth.http.auth_token}} 
# Nota: Questa riga recupera il token salvato dall'ultimo login in auth.http (funzionalità specifica del REST Client)

### 1. Ottenere i dettagli del proprio account (GET /api/users/me)
# DEVE essere eseguito DOPO il login in auth.http
GET {{base_url}}/api/users/me
Authorization: Bearer {{auth_token}}